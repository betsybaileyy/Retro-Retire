<h1>Current Job Listings</h1>

<div class="row">
  {{#each jobs}}
    <div class="col-sm-3">
      <h2><a href="/jobs/{{this._id}}">{{this.title}}</a></h2>
      <small>{{this.jobTitle}}</small>
    </div>
  {{/each}}
</div>
